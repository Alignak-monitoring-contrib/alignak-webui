define host{
    use                     north-host
    host_name               nsca-host-01
    alias                   NSCA host 1
    display_name            Passively monitored host 1
    address                 0.0.0.0

    # Realm
    ; realm                               North
    ; realm                   All
}

define host{
    use                     north-host
    host_name               nsca-host-02
    alias                   NSCA host 2
    display_name            Passively monitored host 2
    address                 0.0.0.0

    # Realm
    ; realm                               North
}
