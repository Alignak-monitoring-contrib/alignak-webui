# Host and service groups
define hostgroup {
    hostgroup_name      linux-snmp
    alias               Linux SNMP checked hosts group
}

define servicegroup{
    servicegroup_name   linux-snmp
    alias               Linux SNMP checked services
}

define hostgroup {
    hostgroup_name      windows-snmp
    alias               Windows SNMP checked hosts group
}

define servicegroup{
    servicegroup_name   windows-snmp
    alias               Windows SNMP checked services
}
